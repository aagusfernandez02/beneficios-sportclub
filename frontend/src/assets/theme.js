// theme.js o theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FEC400', 
    },
    secondary: {
        main: '#D4000D',
    }
  },
});

export default theme;
